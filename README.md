# Ticket System
Ticket system implementation using queue

Includes Junit tests and edge cases, with random insert/processing simulation of real tickets

# Complexity

O(1) Time complexity
O(1) ~ O(n) Space complexity
